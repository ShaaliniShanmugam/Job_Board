<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Job Board</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha384-k6RqeWeci5ZR/Lv4MR0sA0FfDOMe8U9qFhI+H3LL5jj3yFZZgPOlD/e5S6kHCAH" crossorigin="anonymous">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: #ffffff;
            overflow-x: hidden;
        }

        header {
            display: flex;
            justify-content: flex-start; /* Align all content to the left */
            align-items: center;
            padding: 15px 30px;
            background-color: rgba(0, 0, 0, 0.8);
            position: fixed;
            width: 100%;
            z-index: 1000;
        }

        header h1 {
            margin: 0;
            font-size: 24px;
        }

        nav {
            display: flex;
            margin-left: 20px; /* Adds a bit of space between Job Board and links */
        }

        nav a {
            color: #ffffff;
            text-decoration: none;
            padding: 8px 15px;
            border-radius: 5px;
            transition: background-color 0.3s;
            font-size: 16px;
            margin-left: 10px; /* Adds minimal spacing between links */
        }

        nav a:hover {
            background-color: #007bff;
        }

        .container {
            max-width: 1200px;
            margin: 100px auto 0;
            padding: 20px;
            text-align: center;
        }

        .hero {
            padding: 100px 20px;
            animation: fadeIn 1s;
        }

        .hero h2 {
            font-size: 48px;
            margin-bottom: 20px;
            animation: slideIn 1s forwards;
            opacity: 0;
            transform: translateY(-50px);
        }

        .hero p {
            font-size: 20px;
            margin-bottom: 30px;
            animation: slideIn 1s forwards;
            opacity: 0;
            transform: translateY(-50px);
            animation-delay: 0.2s;
        }

        .buttons {
            margin-top: 30px;
        }

        .buttons button {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
            margin: 10px;
            transition: background-color 0.3s;
        }

        .buttons button:hover {
            background-color: #218838;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateY(-50px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <header>
        <h1>Job Board</h1>
        <nav>
            <a href="/login">Login</a>
            <a href="/signup">Sign Up</a>
        </nav>
    </header>

    <div class="container">
        <div class="hero">
            <h2>Find Your Dream Job</h2>
            <p>Welcome to the Job Board, your go-to platform for job seekers and employers. Connect, explore opportunities, and launch your career!</p>
            <div class="buttons">
                <button onclick="location.href='/signup'">Get Started</button>
                <button onclick="location.href='/login'">Login</button>
            </div>
        </div>
    </div>
</body>
</html>
